import { FSConfiguration } from './fs.configuration';

export const configuration: FSConfiguration = {
    apiUrl: 'http://localhost:9850',
    environment: 'dev',
    production: false,
    version: '1.0.0',
};
